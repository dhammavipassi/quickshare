# u73afu5883u53d8u91cfu793au4f8bu6587u4ef6
# u590du5236u6b64u6587u4ef6u4e3a .env u5e76u6839u636eu9700u8981u4feeu6539

# u73afu5883u8bbeu7f6e: development, production, test
NODE_ENV=development

# u5e94u7528u7aefu53e3
PORT=5678

# u65e5u5fd7u7ea7u522b
LOG_LEVEL=dev

# u8ba4u8bc1u8bbeu7f6e
AUTH_ENABLED=true
AUTH_PASSWORD=admin123

# Cloudflare 命名隧道（推荐将 token 写在本地 .env*，不要提交到仓库）
# CF_TUNNEL_TOKEN=

# 数据存储（二选一）
# 1) 本地/Docker：使用 SQLite 文件（默认路径按环境自动选择）
# 可显式指定文件位置（推荐映射至持久化卷路径）：
# DB_PATH=./db/html-go.db

# 2) Vercel/云端：使用 Postgres（Neon/Supabase 免费层）
# 设置 DATABASE_URL 后将自动启用 Postgres + cookie 会话（无状态）
# 例：postgres://user:pass@host/db
# DATABASE_URL=

# Postgres 连接 SSL（Neon/Supabase 推荐开启）。设为 1 可禁用 SSL。
# PGSSL_DISABLE=0

# 会话设置
# Vercel/存在 DATABASE_URL 时默认使用 cookie-session；也可手动指定：
# SESSION_STRATEGY=cookie   # or file
# SESSION_SECRET=please-change-me

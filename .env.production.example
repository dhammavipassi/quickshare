NODE_ENV=production
PORT=3003
AUTH_ENABLED=true
AUTH_PASSWORD=changeme
# 将数据库持久化到挂载卷（见 docker-compose.yml 中 html-go-data 卷）
DB_PATH=/usr/src/app/data/html-go.db

# 或在 Vercel/云部署中改用 Postgres（推荐，持久化）
# DATABASE_URL=postgres://user:pass@host/db
# SESSION_STRATEGY=cookie
# SESSION_SECRET=please-change-me

# 说明：
# - 本文件用于示例与占位，请复制为 .env.production 并填写真实值；
# - 请勿将 .env.production 提交到公共仓库；
# - Cloudflare 隧道 Token 请放置于项目根目录 .env（Compose 变量插值读取）。
